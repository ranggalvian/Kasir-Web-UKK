export interface Kategori {
    id_kategori: number;
    nama_kategori: string;
    created_at?: string; // Tanggal pembuatan (opsional)
    updated_at?: string; // Tanggal update terakhir (opsional)
}
